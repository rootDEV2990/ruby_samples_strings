# Concat with poerator "+" plus operator
p "join these strings " + "with plus operator"
# Concat with poerator "<<" shovel operator
p "join these strings " + "with shovel operator"
# Concat with ".concat" methodshovel operator
p "join these strings ".concat("with concat method")